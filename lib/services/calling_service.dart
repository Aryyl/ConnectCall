/// Encapsulates all ZEGOCLOUD calling operations.
///
/// Phase 6: Full implementation.
/// All ZEGOCLOUD SDK calls are contained here — never called directly from UI.
///
/// The public API is intentionally generic:
///   startAudioCall(), startVideoCall(), endCall(), toggleMute(), etc.
/// This makes future SDK migration straightforward.
class CallingService {
  // Phase 6: Will hold ZegoUIKitPrebuiltCallInvitationService instance.
}
